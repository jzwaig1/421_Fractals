**The build tools we used require VS2019**

To run the solution open 421_Fractals.sln

Once in Visual Studio navigate to line 167 of
421_Fractals.cpp

Uncomment the desired number of Fractal drawing calls
with the desired implementation as described in the comment.

Running both implementations in the same runtime can result
in bad window states and is discouraged.

Run the solution.